import React from 'react';
import JokeGenerator from '../components/JokeGenerator';

/**
 * JokeGeneratorPage - Dedicated page cho Joke Generator feature
 * Co the su dung voi React Router sau
 */
const JokeGeneratorPage: React.FC = () => {
  return <JokeGenerator />;
};

export default JokeGeneratorPage;
